package main

import (
	"testing"
)

func TestBasicClassPrimitive(t *testing.T) {
	// Skip this test for now as it's causing issues with the garbage collector
	t.Skip("Skipping basicClass test until garbage collector issues are fixed")
}
